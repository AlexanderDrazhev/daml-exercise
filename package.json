{
  "name": "create-daml-app",
  "version": "0.1.0",
  "private": true,
  "description": "DAML app with React UI and NestJS backend gateway",
  "scripts": {
    "start": "npm run start:all",
    "start:all": "concurrently -n backend,ui -c magenta,green \"npm run start:backend\" \"npm run start:ui\"",
    "start:daml": "daml start",
    "start:backend": "npm run start:dev --prefix backend",
    "start:ui": "npm run start --prefix ui",
    "build": "npm run build --prefix backend && npm run build --prefix ui",
    "build:backend": "npm run build --prefix backend",
    "build:ui": "npm run build --prefix ui"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
